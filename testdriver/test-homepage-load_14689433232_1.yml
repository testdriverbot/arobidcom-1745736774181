version: 4.2.29
session: 680dd773715f68d750e81aef
steps:
  - prompt: Open the website.
    commands:
      - command: focus-application
        name: Google Chrome
      - command: press-keys
        keys:
          - f5
  - prompt: Assert that the homepage loads successfully.
    commands:
      - command: assert
        expect: the text "arobid.com B2B Marketplace" is visible on the page
  - prompt: Assert that website logo is visible.
    commands:
      - command: assert
        expect: the text "arobid" is visible near the logo
